import React from 'react';
import PropTypes from 'prop-types';
import styles from './Detail.module.css';

function Detail(props) {
    return (
        <div className={styles.Detail}> Detail Component </div>
        )
  };

Detail.propTypes = {};

Detail.defaultProps = {};

export default Detail;
